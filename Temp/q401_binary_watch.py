# Time:  O(n)
# Space: O(1)

# 解题思路：
# 拼成一个数字10位2进制数，然后获取所有的可能性，然后时分分别转换为有效的字符串，再进行拼接即可
# 效率更高的方式是，先验证有效性再生成数字


class Solution:
    def readBinaryWatch(self, num: 'int') -> 'List[str]':
        return None
